#!/bin/bash
if [[ "$OSTYPE" =~ "darwin" ]]; then
  open -a /Applications/Sublime\ Text\ <%= node['sublime_text']['version'] %>.app $@
else
  /usr/local/bin/subl<%= node['sublime_text']['version'] %> $@  </dev/null >/dev/null 2>&1 &
fi
